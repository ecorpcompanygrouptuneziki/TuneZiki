//ROUTE TO CATERGORIES PAGE
CATERGORIESPAGENAV = () => {
      // Get the target element's ID you want to scroll to
      const targetId = "CatergoriesPage";
      // Scroll to the target element
      const targetElement = document.getElementById(targetId);
      if (targetElement) {
        targetElement.scrollIntoView({ behavior: "smooth" });
      }
    
};

//ROUTE TO MUSIC PAGE
MUSICPAGENAV=()=>{
    // Get the target element's ID you want to scroll to
    const targetId = "MusicPage";
    // Scroll to the target element
    const targetElement = document.getElementById(targetId);
    if (targetElement) {
      targetElement.scrollIntoView({ behavior: "smooth" });
    }
}
  
//ROUTE TO USERACCOUNT PAGE
USERACCOUNTPAGENAV=()=>{
     // Get the target element's ID you want to scroll to
     const targetId = "UserAccountPage";
     // Scroll to the target element
     const targetElement = document.getElementById(targetId);
     if (targetElement) {
       targetElement.scrollIntoView({ behavior: "smooth" });
     }
}

//ROUTE BACK TO HOME
BACKTOHOME=()=>{
    // Get the target element's ID you want to scroll to
    const targetId = "HomePage";
    // Scroll to the target element
    const targetElement = document.getElementById(targetId);
    if (targetElement) {
      targetElement.scrollIntoView({ behavior: "smooth" });
    }
}

//ROUTE TO MY PROFILE PAGE
MYPROFILE=()=>{
    // Get the target element's ID you want to scroll to
    const targetId = "MyProfileData";
    // Scroll to the target element
    const targetElement = document.getElementById(targetId);
    if (targetElement) {
      targetElement.scrollIntoView({ behavior: "smooth" });
    }
}

//ROUTE TO SETTINGS PAGE
SETTINGS=()=>{
  // Get the target element's ID you want to scroll to
  const targetId = "MySettings";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}
//ROUTE TO PRIVACY POLICY PAGE
PRIVACYPOLICY=()=>{
     // Get the target element's ID you want to scroll to
  const targetId = "PrivacyPolicyPage";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//ROUTE TO HELP PAGE
HELPPAGE=()=>{
      // Get the target element's ID you want to scroll to
      const targetId = "HelpPage";
      // Scroll to the target element
      const targetElement = document.getElementById(targetId);
      if (targetElement) {
        targetElement.scrollIntoView({ behavior: "smooth" });
      }
}