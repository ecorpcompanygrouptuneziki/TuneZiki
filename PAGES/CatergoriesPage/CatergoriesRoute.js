//ROUTE TO GOSPELSECTION
GOSPELSECTION=()=>{
    // Get the target element's ID you want to scroll to
  const targetId = "MyGospelSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//ROUTE TO HIPPOPSECTION
HIPPOPSECTION=()=>{
    // Get the target element's ID you want to scroll to
  const targetId = "MyHippopSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//ROUTE TO TRAPSECTION
TRAPSECTION=()=>{
    // Get the target element's ID you want to scroll to
  const targetId = "MyTrapSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//ROUTE TO AFROSECTION
AFROSECTION=()=>{
    // Get the target element's ID you want to scroll to
  const targetId = "MyAfroSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//ROUTE TO OLDIESSECTION
OLDIESSECTION=()=>{
    // Get the target element's ID you want to scroll to
  const targetId = "MyOldiesSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}

//BACK TO CATERGORIESPAGE
BACKTOCATERGORIES=()=>{
      // Get the target element's ID you want to scroll to
  const targetId = "MyCatergoriesSection";
  // Scroll to the target element
  const targetElement = document.getElementById(targetId);
  if (targetElement) {
    targetElement.scrollIntoView({ behavior: "smooth" });
  }
}